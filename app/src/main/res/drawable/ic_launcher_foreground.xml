<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Using a placeholder to represent the image, as I cannot directly convert PNG to Vector path here.
         However, for the best result, you should use the Image Asset Studio as mentioned before.
         If you want to use the PNG directly as the icon, we can update the mipmap XML. -->
</vector>